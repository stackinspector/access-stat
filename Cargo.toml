[package]
name = "access-stat"
version = "0.1.0"
edition = "2018"
authors = ["stackinspector"]
license = "MIT"
repository = "https://github.com/stackinspector/access-stat"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
structopt = "0.3"
